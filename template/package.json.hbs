{
  "name": "{{project.name}}",
  "version": "{{project.version}}",
  "description": "{{project.description}}",
  "main": "lib/index.js",
  "author": "{{project.author}}",
  "license": "{{project.license}}",
  "dependencies": {
    "neon-cli": "{{project.neonCliVersion}}"
  },
  "scripts": {
    "install": "neon build"
  }
}
